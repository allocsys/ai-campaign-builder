izen# Backend API base URL for production builds.
# This is the default Cloudflare workers.dev URL for apps/backend -- a stopgap
# until a real custom domain is decided (see architecture.md / open items on
# business domain naming). Vite picks this up automatically for `vite build`.
VITE_API_BASE_URL=https://ai-campaign-builder-backend.pachoolai24.workers.dev
